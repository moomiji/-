name: Update GITHUB_ENV Variable

on: [push]

jobs:
  update-env:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set initial value
        run: wget https://raw.githubusercontent.com/MaaAssistantArknights/design/main/logo/maa-logo_128x128.png



name: Update GITHUB_ENV Variable

on: [push]

jobs:
  update-env:
    runs-on: windows-latest

    steps:
      - name: Set initial value
        shell: bash
        run: curl -OL https://raw.githubusercontent.com/MaaAssistantArknights/design/main/logo/maa-logo_128x128.png



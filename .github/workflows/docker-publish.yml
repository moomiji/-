name: Update GITHUB_ENV Variable

on: [push]

jobs:
  update-env:
    runs-on: windows-latest

    steps:
      - uses: moomiji/-/.github/actions@master
        id: test
      - if: steps.test.outputs.failed
        run: echo ${{ steps.test.outputs.failed }}

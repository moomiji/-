name: Update GITHUB_ENV Variable

on: [push]

jobs:
  update-env:
    runs-on: windows-latest

    steps:
      - uses: ./.github/actions/publish_nodejs.yml
        with:
          access: public
          token: 11111
          dry-run: true
